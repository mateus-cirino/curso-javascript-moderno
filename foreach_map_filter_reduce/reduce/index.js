// Reduce

const arr = [1, 2, 3];

console.log(arr.reduce((anterior, atual) => anterior + atual));

// Outros usos

const frase = ["Outros", "usos"];

console.log(frase.reduce((anterior, atual) => anterior + " " + atual))